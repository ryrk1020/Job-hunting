# Pinned versions for reproducible builds. Bump intentionally and
# re-test the full pipeline; do not loosen to >= without a reason.
requests==2.32.3
urllib3==2.2.3
PyYAML==6.0.2
python-dateutil==2.9.0.post0
feedparser==6.0.11
jinja2==3.1.4
